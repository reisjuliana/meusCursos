const a = 7 // atribuicao
let b = 3  // atribuicao

b += a // somando a em b / b = b + a
b -= a // subtraindo a em b / b = b - a
b *= a // multiplicando a em b / b = b * a
b /= a // subtraindo a em b / b = b / a
b %= a // subtraindo a em b / b = b % a